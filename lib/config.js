// /lib/config.js
// Central app config. TODO: move to DB settings in prod if you want admin control.
const config = {
  REVIEW_WINDOW_DAYS: 7, // days from 'delivered' to auto-complete
};
export default config;
